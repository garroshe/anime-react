import error from './error.gif';

const ErrorMessage = () => {
    return <img src={error} alt="error" style={{'width': '250px', 'display': 'block', 'margin': '0 auto'}}/>
}

export default ErrorMessage;